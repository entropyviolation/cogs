/**
 * app/loading.tsx — Route-level loading fallback
 *
 * Next.js App Router `loading` boundary for the root route. Renders nothing
 * (the app mounts fast and manages its own per-panel Suspense fallbacks).
 */
export default function Loading() {
  return null
}
