================================================================================
SCREENSHOT: 05-lists-content-default.png
VIEW: Lists tab → Example List → Default display
AREA: Lists → list → Default
SOURCE FILES:
  - components/Lists/list-content/ListContentDefault.tsx
  - components/Lists/list-content/ListContentPanel.tsx
================================================================================

GLOBAL CHROME (all screens)
  Pinned full-width mill title bar (`AppHeader`, sticky top, z-40): navy BRAIN2 caption with Tek POWER lamp + today's-friend jewel (chrome + black-mirror well; click for a Stardew to-do bubble) and Friend / Review / System / optional now well (between System and Capture; idle → hidden; live Working sessions show name, elapsed, Stop, Pause↔Resume) / Capture as Win95 groupboxes of milled press keys. Names stays Names and latches (sunken); tooltip Stop naming while on. Phosphor counts on Review and Inbox. Quick Add is the default framed key. Same doors as before — grouped furniture, not a menu bar.
  Tabs: Home · Lists · Docs · Scheduler · Operations · Modules · Analytics.
  Shortcuts: Cmd/Ctrl+K search · capture hotkey → Quick Add.

VIEW-SPECIFIC
Open-list chrome: Up, New List/Folder, Import, Completed, Missed Opportunities, Settings, Select, Display: Default / Checklist / Icons / Details / Spreadsheet, Search. Address. Sidebar. Inner caption bar = display mode (Default), not the list name again. Bulk add items, Add Item. Default: reading rows (round status pip + tiny orb + name + type/priority/meta) with no complete checkboxes; Select mode overlays selection ticks. Click a row → detail. Right inspector: Count / Type / Last touched, then actions (Add Task, Bulk add, List Settings, Change Icon, Pin). **Delete List is not in the inspector** — only List Settings → Dangerous actions (Clear list + red Delete). Status: This folder: N item(s) · Tree: N folder(s), M list(s) · Smart lists. Type floor 10px; inspector/status/Default names 12px.
