================================================================================
SCREENSHOT: 09-modules.png
VIEW: Modules tab → dashboard
AREA: Modules dashboard
SOURCE FILES:
  - components/Modules/modules-panel.tsx
  - components/Modules/modules-chrome.css
  - components/Modules/module-bodies.tsx
  - components/Modules/ModuleConfigDialog.tsx
================================================================================

GLOBAL CHROME (all screens)
  Pinned full-width mill title bar (`AppHeader`, sticky top, z-40): navy BRAIN2 caption with Tek POWER lamp + today's-friend jewel (chrome + black-mirror well; click for a Stardew to-do bubble) and Friend / Review / System / optional now well (between System and Capture; idle → hidden; live Working sessions show name, elapsed, Stop, Pause↔Resume) / Capture as Win95 groupboxes of milled press keys. Names stays Names and latches (sunken); tooltip Stop naming while on. Phosphor counts on Review and Inbox. Quick Add is the default framed key. Same doors as before — grouped furniture, not a menu bar.
  Tabs: Home · Lists · Docs · Scheduler · Operations · Modules · Analytics.
  Shortcuts: Cmd/Ctrl+K search · capture hotkey → Quick Add.

VIEW-SPECIFIC
Modules catalog on the PCB (`.mod95`): gunmetal header strip with **Modules** + default **Build module**. Workspaces as miniature Win95 windows (navy caption, pop-out caption button, description, status well with view-kind glyphs + “N views”). Dashboard widgets as gadget wells (toolbar gear/×; analytics stats in a phosphor CRT well). Empty states are sunken wells. Same actions as before (open, pop out, configure, remove). Builder: scratch / saved defs / templates (Itinerary, Cleaning, Budget, Book Tasting, Film DNA, Blank) or New widget.
