# `lib/` — Data model, state stores, and pure helpers

This folder is the **nervous system** of a living perfect second brain: one
Item graph (capture once; use in as many rooms as possible), one persist path,
one vocabulary that every future installed room inherits. **Analytics** reads
this mass; it does not invent a second ontology.
Everything that is **not** React UI: the TypeScript data model, Zustand stores
(persisted to localStorage — the offline-first source of truth; see
`docs/SPEC_MAPPING.md` §3 for the
planned opportunistic **MongoDB Atlas** sync target — flexible documents,
semantic/fuzzy/advanced search, and aggregation-based routing), and pure
calculation/date/sync utilities.

> **Future direction — shared `@brain2/core`.** Most of this folder (the data model
> in `types.ts`, Zod schemas in `data/schemas.ts`, the `DataSource` interface,
> domain `services/`, and pure logic like `search`, `needs-attention`, `links`,
> and scheduling) is slated for extraction into a shared
> **`@brain2/core`** package so web, desktop, and a future **mobile** (Expo / React
> Native) app consume one source of truth. The `DataSource`/repository seam is the
> **transport-agnostic boundary** that makes offline-first + opportunistic cloud
> sync (and the mobile client) possible. See [`docs/SPEC_MAPPING.md`](../docs/SPEC_MAPPING.md) §3.

> **Why `types.ts` is load-bearing.** The long-term goal is that any small app can
> be **installed** into Brain2 and have its records become ordinary Items
> ([`docs/MODULE_PLATFORM.md`](../docs/MODULE_PLATFORM.md)). Every installed
> module inherits this folder's vocabulary, so the duplicate `title` /
> `description` and `category` / `categories` axes in
> [`docs/CANONICAL_FIELDS.md`](../docs/CANONICAL_FIELDS.md) are not cosmetic
> debt — they are the thing a port wizard would have to map onto twice. Settling
> them, and settling the single write path that workflows / implied actions /
> undo / persist all observe, comes before further UI work.

## Zustand stores

localStorage keys are **`brain2-*`**. Historical **`cogs-*`** keys are a lossless
alias (`storage-keys.ts`): copy onto `brain2-*` when missing; small pins still
dual-write; large vaults write `brain2-*` only so origin quota is not doubled.
Oversized duplicate `cogs-*` twins are dropped on boot once `brain2-*` exists.
Settings **Data profile** can switch **Live** (default)
vs **Demo**; Demo physical keys are **`brain2-demo-*` only** (`data-profile.ts`,
`demo-vault.ts`) and never dual-write Live. Bytes — attachments, pasted pictures,
Docs HTML — belong in IndexedDB, not in a store's JSON: the origin holds ~5MB for
*all* keys together, so one picture in a Lists attribute can stop Tracking from
saving. IndexedDB (`cogs-attachments`, `cogs-docs` live;
`brain2-demo-docs` / `brain2-demo-attachments` on Demo), CSS
(`.cogs-color-swatch`), and Electron userData folder `cogs` are not persist keys
and stay. The product name is **Brain2** / **BRAIN2**.

| File | localStorage key | Purpose | Spec |
|------|------------------|---------|------|
| `task-store.ts` | `brain2-task-storage` | **Item records** (historical field `tasks[]`), lists, folders — the Inbox / Lists / Scheduler source of truth. Date-aware serialization (`DATE_KEYS`, matched at any depth), versioned migrations. Persist **v12** fills a missing `type` (Next Actions / inbox → `"task"`, else `"item"`) without overwriting explicit types. `addItem`/`updateItem`/`deleteItem`/`getItems` alias `addTask`/`updateTask`/`deleteTask`/`tasks`. `addTask`/`updateTask` keep `title` current via `syncTitleFromDescription` and expand **connected-list** membership (`addListLink` / `removeListLink`, `lib/list-links.ts`). `updateList` preserves `linkedTargetListIds`. Hard deletes stamp **`removedTaskIds`** so hub / phone-hub id-unions cannot resurrect a row after a deliberate delete (Telegram Inbox captures stay via the same union). | §4, §5, §6, §7 |
| `event-store.ts` | `brain2-event-storage` | Calendar `CalendarEvent`s. Seeded with demo events. | §7.5 |
| `planned-action-store.ts` | `brain2-planned-actions` | Day-agenda **planned actions** (timed intentions): free drag-created blocks plus To Do / daily-habit placements. Not events; habit drop does not complete the habit. `cogs-planned-actions` alias. Types + drop helpers in `planned-actions.ts`. | §7.4 |
| `habits-store.ts` | `brain2-habits-store` | Habit definitions (`WeeklyTask`, persisted `gem`), per-day completion data (`WeeklyData`), weekly/monthly period completions, habit categories, **gradeTolerance** / **outputGradeTolerance**, **gradeTubeColor** / **outputGradeTubeColor** (Week/Span vs Perfect output discharge hues), daily **accomplishmentThreshold** / **accomplishmentBonus**, **dayGradeLiftBonus** / **weeklyGradeLiftBonus** (persist **v21**), priority blend toggles, heatmap / **Day View** / **Hide Completed Today** / **Loading Bar** / **Small LEDs** / sort prefs, **willpowerImage**, **habitGems**, **percentLedTint**, **habitsControlPanelWidth**, **willpowerPhysicsHud**, **willpowerPhysics**, **contentRev**. Persist **v19** bumps `contentRev` on title, detail, and completion writes so a late rehydrate cannot restore the previous snapshot; hollow live maps still overlay disk completions so grades keep their checklist. Each completion cell also stores `updatedAt`; a merge keeps a cell the other snapshot lacks, and the newer stamp wins, so a second window's gem stamp cannot uncheck a habit. Persist **v18** resets leftover `willpowerPhysics` knobs to the calm default whirl. Persist **v17** pins control panel width compact (seam retired). Persist **v16** adds control panel width + Willpower gems physics lab. Persist **v15** adds `habitSmallLeds` (default on; Willpower gems satellites are derived from that week’s completions). Daily completions upsert 50×ratio points + accomplishment and grade bonuses (`lib/habit-points.ts`). Climb migrate v3. Done log. `applyTrackedValue` writes the Tracking auto-fill half of a day, week, or month. Manual completions push onto `action-history.ts` (Cmd/Ctrl-Z). | §9 |
| `goals-store.ts` | `brain2-goals-store` | All-time **Objectives** (prioritizable per period with custom point multipliers; written period reviews) + quantifiable **Goals** that serve them. Seeds 26 default objectives + example goals (persist v3). Exports the multiplier helpers `objectiveMultiplierFor`/`taskObjectiveMultiplier` + `DEFAULT_OBJECTIVE_MULTIPLIER` (1.5×). | §10 |
| `points-store.ts` | `points-store` | Points ledger (`PointsEntry[]`), `addPoints` / `upsertPoints` / `removePointsForTask` (drop a task's rows for one local day — completion-popup Undo), day/week/month totals, possible-points projections. Local `YYYY-MM-DD` keys. Inbox handling writes here via `inbox-credit.ts`. | §14 |
| `time-tracking-store.ts` | `brain2-timegrid-store` | TimeGrid views and pens (Activity / Location / Mood / **Company** / **Screen Time** / **iPhone Screen Time** / **iPhone Calls** / **iPhone Texts**), minute-resolution `TimeEntry[]` (`penId` primary + optional `secondaryPenIds`, optional display `title`, optional `kind: "instant"` discrete events, `splitAfter` scissors seams), pen **parents** + **image** mosaic, per-view **displayDepth**, **hidden pens**, **one Infinite scroll** (`infiniteScroll`; v10 migrates the old day/week flags), **Recent** sort, per-pen **variants** and **actionFormats**, cell size and span, the cross-scope **tag** library, per-day **`dayNotes`** (mirrored append-log JSON — the jot itself is `brain2-tracking-day-notes`), **untracked-gap notes**, confirmed Day Log events, and block **`precision`**. Persist **v12** appends iPhone Screen Time / Calls / Texts without switching `activeScopeId`. Persist **v11** appends Screen Time (category roots only) without switching `activeScopeId`. Optional new fields need no bump. `removedEntryIds` records blocks removed on purpose so a union cannot paint them back. `paintMinutes` wraps past midnight. Data writes push onto `action-history.ts`. Screen Time prefs are **not** in this blob. | §12 |
| `day-notes-persist.ts` | `brain2-tracking-day-notes` | Tracking bottom-notes vault. Tiny `YYYY-MM-DD → append-log JSON` map written on submit and hub-synced on that key (shrink-protected). Not rewritten into the huge timegrid blob per submit. **Both aliases are read and unioned by entry id**, and a seed / hub copy of a day already on file unions too, so no copy of an append log is ever the loser — that heals a pair split by a quota-failed write. | §12 |
| `screentime/prefs.ts` | `brain2-screentime-prefs` | ActivityWatch connection + sync knobs (URL, lookback, min duration, window titles off by default, last success, honest `lastSyncNote`). Dedicated key — not inside the timegrid blob. | §12 |
| `work-session-store.ts` | `brain2-work-session` | At most one live Operations **"working on this now"** session (`operationId` + start time + Tracking entry ids). Start/stop/tick live in `operation-work-session.ts`. | §5, §12 |
| `pen-color-session-store.ts` | `brain2-pen-color-session` | At most one live pen-color **"working on right now"** session (pen, scope, start second, Tracking entry ids). Start/stop/tick live in `pen-color-session.ts`. Independent of the Operations session. | §12 |
| `reviews-store.ts` | `brain2-reviews-store` | Period reviews (day/week/month/quarter/year) plus helpers for period keys, previous/next period, and carry-over prompts. | §13 |
| `modules-store.ts` | `brain2-modules-store` | User-composed Modules: dashboard **widgets** (`{ id, type, title, config }`) and full-screen **workspaces** (`kind: "workspace"` + bound `views[]`, `planSync`, `enablePrint`). | §8 |
| `item-type-store.ts` | `brain2-item-types-store` | Registry of **item types**. System types (Task, Item, Note, Operation) are always re-seeded from code. Catalog types (Book, Furniture, Resource, Shopping, Flight) are seeded once; user edits persist (`mergeTypeRegistry`). Persist **v2** `migrate` re-merges older snapshots so Zustand does not drop the blob. | §5 |
| `lists-ui-store.ts` | `brain2-lists-ui` | Lists UI prefs: folder view mode, icon positions, per-location `iconLayoutMode` (`auto` pack vs `freeform` freeze), orb gallery (hidden orbs, edit mode), auto-organize grid, All Items list/folder checkbox filters (including Select all / Deselect all). Persist **v5** adds `iconLayoutMode`. | §6 |
| `home-widgets-store.ts` | `brain2-home-widgets` | Home overview tile visibility + order (one blob for every Home sub-tab, persist **v6** shows Latest award after Points; **v5** tucks Night well, Harvest leftover, and Inbox mill). Catalog + sanitize live in `home-widgets.ts`. Included in the Settings full backup. | §8 |
| `home-days-until-store.ts` | `brain2-home-days-until` | Date + label for the Days Until countdown. Included in the Settings full backup. | §8 |
| `home-weather.ts` | Widget city/beach sanitize (`brain2-home-weather`), rain-chance copy, advisories, human forecast sentence, weekday labels. | §8 |
| `home-weather-store.ts` | `brain2-home-weather` | Last city + NOAA beach for the Home weather instrument. Settings `homeCity` is fallback only. Included in the Settings full backup. | §8 |
| `metrics-store.ts` | `brain2-metrics-store` | Custom self-tracking metrics: `MetricDefinition`s (name, unit, `MetricKind`, target/bounds, color) + dated `MetricEntry` readings. Types are local (not in `types.ts`) so the feature stays self-contained. Backs Metrics analytics + `MetricLogger`. | §15 |
| `regret-store.ts` | `regret-store` | Append-only `RegretEntry[]` ledger mirroring `points-store` for the *opposite* signal — the accruing cost of not doing important/overdue items. Pure accrual math (`regretCost`/`dailyRegretIncrement`/`projectedRegret`) is exported; daily accrual is idempotent per task per day. | §14 |
| `theme-store.ts` | `brain2-theme-store` | User-customizable theme colors (points card, habit-type icons), **`chromeFace`** (0–100 gunmetal set-point, default 50 = classic `#c0c0c0`), and **`pcbMode`** (desktop field: teal / ceramic / mint / ice / xray / fr4, default **teal**). Persist **v4**: blobs that never stored `pcbMode` keep ceramic; a stored plate is kept. **`appearanceRev`** is stamped from `appearance-rev.ts` (wall-clock) on each plate/hue pick, so a pick wins the merge even when it lands before rehydration finishes. A blob with `appearanceRev` > 0 wins over a stale **`brain2-pcb-mode`** pin (Electron used to paint the pin back onto the plate on launch). The pin is restamped from that blob so the boot script matches. | — |
| `user-settings-store.ts` | `brain2-user-settings` | Cross-cutting prefs: home city for Plan sunrise/sunset (default San Diego) and `dayAnchorMinutes`, the time of day autogenerated completions fall back to for a day that is already over (default 9:00 PM). | — |
| `sun-times-store.ts` | `brain2-sun-times` | Per-day sunrise/sunset cache keyed by `YYYY-MM-DD\|lat\|lng`. First write for a calendar day sticks; later pins or "today's" clock do not overwrite historical rows. Astronomy is `sun-times.ts`. Included in the Settings full backup. | §12 |
| `ui-names-store.ts` | `brain2-ui-names` | Overlay mode machine (`off` / `names`; Help / Inspect can join later). Names is the first `data-ui-mode`. Included in the Settings full backup. DOM catalog is `data-ui-name` / optional `data-ui-help` / `data-ui-docs` (living README path) / optional `data-ui-docs-anchor`. | — |
| `sleep-store.ts` | `brain2-sleep-store` | Persisted nightly log: morning date → night, plus the nightly target. Holds only what the user stated; everything derived from it lives in `sleep-sync.ts` and `sleep-inference.ts`. Night writes are undoable via `action-history.ts`. | §12 |
| `workflows-store.ts` | `brain2-workflows-store` | User-authored `WorkflowDefinition`s (trigger → conditions → actions), module-scoped or global. Mirrors the `modules-store` persist pattern; run by `services/item-mutation-service.ts`. | §11 |
| `module-definitions.ts` | `brain2-module-definitions` | Saved `ModuleDefinition` blueprints (rung 1 of the install ladder — bound lists + attribute extensions + views + workflows), plus export/import to JSON. See [`docs/MODULE_PLATFORM.md`](../docs/MODULE_PLATFORM.md). | §11 |
| `ingest/ingest-store.ts` | `brain2-ingest-store` | Phone-message ingest: pairing code, Telegram allowlist, custom shortcuts, always-on hub URL, pinned grocery message ids, applied-message log. Poll timestamps stay in memory so a 3s tick cannot rewrite the persist hub. Bot token is **not** here (Electron `safeStorage`). | §4 |

## Pure helpers (no store)

| File | Purpose | Spec |
|------|---------|------|
| `unsaved-changes.ts` | Dirty snapshot compare (`serializeSnapshot` / `snapshotsEqual`). Dates as ISO. Pair with `components/ui/unsaved-changes-guard.tsx`. House law in `docs/DESIGN_STYLE.md`. | — |
| `app-brand.ts` | Product name: chrome/window **BRAIN2**, prose **Brain2**. Persist keys are **`brain2-*`**; **`cogs-*`** is a lossless alias. | — |
| `storage-keys.ts` | `persistKey`, aliased read/write, `copyLegacyPersistKeys`, `releaseDuplicateLegacyTwins`, `releaseCopiedUnprefixedKeys`, `releaseFriendPicLocalKeys`, `isQuotaExceededError`. Canonical prefix `brain2-`. Live dual-writes small `cogs-*` pins; large vaults are `brain2-*` only. A write lands on the **first alias — the one reads prefer — before any twin, and that write alone decides success**: a half-failed pair on a full origin parked the newer value on `cogs-*` where nothing reads it, so a submitted day note read back as never written. `releaseDuplicateLegacyTwins` now skips a twin that *drifted* from its `brain2-*` copy (it may hold that newer value). QuotaExceeded reclaims duplicate twins **and dead pre-prefix relics** (`task-storage`, `habits-store`, `timegrid-store`, … — 130KB the live vault could not spend) and writes again; a bare key with no `brain2-*` copy is the only vault and stays. Oversized friend-pic data URLs are dropped on boot. Demo remaps to `brain2-demo-*` only. Selector `brain2-data-profile`. | — |
| `data-profile.ts` | Switch Live ↔ Demo (reload). Reset Demo wipes only `brain2-demo-*` + demo IndexedDB. | — |
| `demo-vault.ts` | Stock fiction persona (River Hale) covering lists, ops, books, flights, notes, sources/beliefs, habits, tracking, sleep, plans, goals, metrics, reviews. Seeds only after Demo is selected. | — |
| `chrome-patina.ts` | One cool gunmetal for every chrome surface. `--chrome-face` / `--chrome-hi` / `--chrome-mid` / `--chrome-lo` stay chroma-0 gray (never brown). Settings set-point (0–100, default 50 = `#c0c0c0`); live value drifts ±5 RGB over 24 minutes. | — |
| `appearance-rev.ts` | `nextMonotonicRev` / `nextAppearanceRev` / `nextContentRev` — the monotonic **wall-clock** stamp. Plate / hue picks use `appearanceRev`. Habit titles, details, and completions use `contentRev` (persist v19). A plain counter restarted at 0 each launch, so a pick made before persist rehydration finished (Electron's `getItem` waits on the hub) lost the merge against the stored rev and was written back onto the pins — the same race was restoring old habit names and cell values. | — |
| `pcb-backdrop.ts` | Desktop field modes (`teal`, `ceramic`, `mint`, `ice`, `xray`, `fr4`). Fresh default is plain Win95 teal. Parses ids, maps ink contrast, stamps `data-pcb-mode` / `data-pcb-ink` on `<html>`, pins the pick on **`brain2-pcb-mode`**, remembers this page's pick on **`brain2-pcb-pick`** (cleared on a real reload), and ships the first-paint boot script (Demo reads `brain2-demo-pcb-mode`; Live reads the pin then `brain2-theme-store` / `cogs-theme-store`). Photographed plates live in `public/pcb/`. | — |
| `baby-animals.ts` | Fixed cute-name list for the header **today's friend** (`Baby Crow`, `cute small kitten`, `sweet little skunk`, … — never “wee”). `localMondayKey` is the week stamp. | — |
| `friend-photo-vault.ts` | Per-card picture bytes in IndexedDB (`idb:friend_<id>`). `friend:<id>` refs in gallery JSON. Oversized `brain2-friend-pic:` localStorage copies are stripped on boot so Inbox and theme can save. | §8 | — |
| `baby-animal-photos.ts` | Friend picture record + uploaded-photograph knockout into the friend vault. **No web search** — `via` is `pack` / `upload` (older cards may still read `openverse` / `commons` / `wikipedia`). | — |
| `friend-pack.ts` | Merge / sort the preapproved `animalsrcs/` gallery (unnamed cards first). Missing pack ids are skipped when dismissed. No store import (avoids a cycle). | — |
| `friend-gallery.ts` | Card identity: names live on `photo.id`. Pack dismiss is id + URL only (a name like “bunny” never steals the catalog bunny). Persist/hub reconcile prefers a named local card over a blank hub twin. | — |
| `friend-pack-manifest.ts` | Generated list of 71 `/friend-pack/*.png` files (`knock` = studio plate removed). | — |
| `baby-animal-greeting.ts` | Worn-friend pin (`brain2-friend-worn`), dismiss pin (`brain2-friend-dismissed`, union-only), visit history, and reunion lines (`Hi again` / `remember me?`) — greet only on a new assignment of a known friend, never on refresh. | — |
| `baby-animal-friend.ts` | Wear / name / upload for the friend. **Pictures only come from the preapproved pack or a human upload — nothing is fetched.** Shuffle wears another gallery card; naming wears the card that already has that name, else names the next unnamed pack card. Auto-roll only on a new Monday (`ensureWeeklyFriend`); empty `weekKey` keeps the worn friend. Dismissed **card ids** stay gone. | — |
| `baby-animal-nudge.ts` | Nest-click adapter: `pickFriendTodoNudge` → `lib/friend-suggestion.ts` (flavor on in the nest). | — |
| `friend-personality-fits.ts` | Name + photograph voice for the current named gallery (bat-as-mouse, turtle-as-skunk, snow marten hyrax, Minecraft axolotl). | — |
| `baby-animal-personality.ts` | Per-species source bias, quirks (love-you, whims, title loves, dialog effects), stored overlay on top of fits. | — |
| `friend-suggestion.ts` | Score + weighted pick over unmet daily habits, day's To Do, open Next Actions, plus flavor. | — |
| `friend-copy.ts` | Tone × source Stardew lines (including affection + whim). | — |
| `friend-whims.ts` | Soft real-world missions (draw, read, walk…). | — |
| `friend-mission.ts` | Mission journal sanitize (cap 80): offered, accepted until local midnight, declined with a reason, done, expired. Breakdown and first-step notes. `friendMissionItemId` opens the item popup over the sheet. | — |
| `friend-reward.ts` | Friend mission points through `points-store` × `rewardScale`. | — |
| `friend-copy.ts` | Tone × source Stardew lines for the bubble. | — |
| `baby-animals-store.ts` | Persisted gallery + current friend (`brain2-baby-animals-store`, persist **v7** card-id identity). Worn name always copies from the current card. Removed ids + source URLs stay dismissed (pin + hub overlay). | — |
| `types.ts` | Shared interfaces/enums. **Ontology:** every row is an Item. **Runtime:** `Task` is still the persisted document; prefer `ItemRecord` (`export type ItemRecord = Task`) on new signatures. Task-the-kind is `type: "task"` + Next Actions. Also `TodoItem`, `CalendarEvent`, lists/folders, habits, scheduling, reviews, modules, attributes. Day-agenda planned actions live in `planned-actions.ts` (not this file) so they stay distinct from `CalendarEvent`. | §5 |
| `planned-actions.ts` | Pure planned-action records (`source`: `free` / `todo` / `habit`), clock math, drop + drag-range factories. Persist is `planned-action-store.ts`. | §7.4 |
| `plan-drag.ts` | Plan rail ↔ agenda drag. Writes `text/plain` `brain2-plan:<kind>:<id>` plus a live in-memory payload so Chromium/Electron cannot empty DataTransfer and no-op the drop. Pointer press+move on the rail emits onto `[data-plan-drop]` when native HTML5 drag never starts. | §7.4 |
| `plan-rail-next-actions.ts` | Next Actions on the Plan rail: Lists `taskIsNextAction` + `isAvailableNow` + not scheduled after the shown day/week/month. | §6, §7.4 |
| `calculations.ts` | Habit completion math: day/week %, `calculateWeekToDateGrade` / `calculateWeekToDateOutputGrade` (0% is never curved up), `gradeAsOfForVisibleWindow` (Plan date outside the Habits week falls back so tubes do not stick on "—"). Weekly/monthly analogs `calculatePeriodGrade` / `calculatePeriodOutputGrade` over 7-period windows. Optional `isExempt` drops a period from both sides of the fraction. Climb uses `incremental-habits.ts`. | §9 |
| `habit-exemption.ts` | Exemption wand. Periods that end before the habit existed are waived automatically (`createdAt`, or the `task-{unix ms}` id when that field was never stored). Daily `logExemptions` lift an all-nighter night: evening-before or morning-of. `undefined` uses the name preset; `[]` keeps it off. A click stores waive/require and clears it when it matches the automatic rule, including a log block. Fully waived days leave grade averages. Streak helper skips exempt days without counting or breaking the run. | §9 |
| `habit-connections.ts` | Sleep clock and next-action list connections. Bedtime on a day reads the next morning’s `sleptMin`; wake reads that morning’s `wokeMin`. Met at or before the threshold. A list connection counts done next actions on that list name for the day. Name presets cover bedtime / wake-before and “complete N to-do”. `null` turns a preset off. | §9 |
| `habit-connection-sync.ts` | Pushes those connections into boolean cells (`sleepCompleted`, `listCompleted`) when the sleep log or a finished next action changes. A hand tick stays. Started from `useHabitTrackingSync`. | §9 |
| `habit-points.ts` | Daily habit points: 50 × day completion ratio; user accomplishment bonus (default +50 at ≥80% raw day score, `lib/habit-accomplishment.ts`); per-day grade bonus 100 (either 75%+) / 300 (both); editable lift (default +25 each) when Week grade or Perfect output beats yesterday, and when a weekly-habit grade beats last week. | §9, §14 |
| `habit-accomplishment.ts` | Daily “good day” curve: `accomplishmentThreshold` / `accomplishmentBonus`, column %, Good day streak, last 30. Optional priority blend via `scoreForDay`. Independent of Week grade / Perfect output curves unless that blend is on. | §9, §14 |
| `habit-priority.ts` | Pin, mute, missed-period auto-weight (compounds), sort, and 50% floor blend for grades / Good days. | §9 |
| `habit-sort.ts` | Row order for Habits: default / A–Z / date created / priority / weekly completion %. Migrates the old priority boolean. | §9 |
| `habit-led.ts` | Percent LED tint sanitize (default `#7e14ff`), lamp off/on/partial, rounded `%` text, `percentBarLitCount` (one of 10 lamps per 10%), the **`brain2-habit-led-tint`** pin, and this page's pick on **`brain2-led-pick`**. | §9 |
| `home-widgets.ts` | Home overview catalog (`review`, `points`, `progress`, `pet`, `daysuntil`, `affirmation`, `weather`, `next`, `daylamp`, `solar`, `tracking`, `night`, `harvest`, `inbox`), sanitize/order helpers, v2 migrate (tuck Next + Day lamp), v3 migrate (fold four points wells into `points`), v4 migrate (tuck Solar remainder + Tracking now), v5 migrate (tuck Night well + Harvest leftover + Inbox mill), `petPose` / `dayLampWord` / `pickHomeNext` / `daysUntilCount`, sun/cloud/rain glyphs, later-today glance + tide hint, 3-hour day strip (`pickWeatherDayStrip`), sparkline path + analog needle math, and the 3-stop gradient from Habits `percentLedTint` / `gradeTubeColor` / `outputGradeTubeColor`. | §8 |
| `home-glances.ts` | Faces for Night well, Harvest leftover, and Inbox mill. | §8 |
| `solar-remainder.ts` | Home Solar remainder phases: until sunrise / sunrise / to sunset / sunset / after sunset. Pure. | §8 |
| `tracking-presence.ts` | Current vs last-known Activity / Location / Mood / Company for the Home Tracking now tile; `applyTrackingPresenceUpdate` paints from now through end of day. | §8, §12 |
| `habit-tube.ts` | Grade-tube discharge hues: sanitize, Home overview defaults (`#508b51` week-grade / `#25366a` perfect-output), `dischargePaint` (core/mid/halo/volume/residual from a hex). Persist keys `gradeTubeColor` / `outputGradeTubeColor`. | §9 |
| `incremental-habits.ts` | Daily vs weekly climb: 4-day weekly bump; daily last-log + increment (drops count); persist-v3 split. | §9 |
| `habit-done-log.ts` | When a habit first meets its goal, add a `loggedAction` Done row (`habit-done-{id}-{date}`); remove it if unmarked. The row carries a **duration** and a **clock window** (`deriveHabitCompletion`) rather than a midday placeholder, and refreshes the duration while the goal stays met (3 pages required, 4 written) unless the user confirmed it. | §9 |
| `estimated-values.ts` | Provenance for autogenerated values: `FieldEstimate` flags naming the field (`completedDate` / `startedAt` / `actualDuration`), how it was produced, and the sentence explaining it. Confirmation is sticky — `canRegenerate` stops a later sync from overwriting a number the user confirmed. `tasksAwaitingConfirmation` drives the review queue. `usualDurationMinutes(tasks, likeThis)` is a **display-only** median of observed `timeLogs` / unflagged `actualDuration` on the same title (then named item type). It never reads or writes `estimatedDuration`. | §6, §9, §13 |
| `habit-time-estimate.ts` | How long a habit's period took, best evidence first: the habit's own minutes/hours value → painted Tracking minutes (+ a rate top-up for the hand-logged remainder) → logged amount × `minutesPerUnit` → a flat per-completion length. Only the last two are flagged as assumptions (`precision: "definite"` opts out). | §9, §12 |
| `completion-window.ts` | Where in the day a completion sat: the last painted Tracking run (observed), else "finished just now" for a day still running, else half an hour before that night's bedtime — stated, painted, or the user's usual — and only then the fixed `dayAnchorMinutes`. Clamped to the waking hours so nothing is backdated into hours the user was asleep. Reports which ends were assumed and, for the bedtime, which kind of evidence it rested on. Replaces the old noon-local stamp. | §9, §12 |
| `habit-week-streaks.ts` | Consecutive weeks where a daily habit was done on ≥4 days (on-grid chips). | §9 |
| `action-history.ts` | In-memory last-action undo/redo for Home and Tracking. `rememberWorld` snapshots Tracking entries + sleep log + work session + pen-color session + habit completions + points + tasks; `undoLastAction` / `redoLastAction` restore them. `runAsAction` batches a burst (week fill, start/stop work) into one step; `withoutUndo` silences ticks. Cmd/Ctrl-Z is wired in `hooks/useUndoHotkey.ts`; Tracking adds a capture-phase companion in `components/Home/Tracking/tracking-undo.ts`. Not a per-item ledger — see `item-activity.ts`. | — |
| `item-activity.ts` | Append-only per-item change log (`brain2-item-activity`). Diffs name / complete / schedule / duration / deps / lists on item-detail save. A full Settings backup carries this key in `extras` and writes it back on restore. | §5.5 |
| `time-entries.ts` | `TimeEntry` interval model: minute-resolution spans, primary + optional `secondaryPenIds`, display `title`, variants, block tags, `precision`, `generatedBy` (`sleep` or `screentime`, id = local `YYYY-MM-DD`), midnight-crossing `spanId`, **`kind: "instant"`** discrete events (`startEventId` / `endEventId` on intervals that they bound), **`splitAfter`** scissors seams (same-pen adjacent merge unless cut). `assignedPenIds` / `entryDisplayName` / `isInstant` / `isSleepBlock`. Pure. | §12 |
| `pen-sort.ts` | Palette order: Recent / A–Z / Tree. `lastUsedFromEntries` backfills from paint. Pure. | §12 |
| `pen-tree.ts` | Parent/child rollup for Tracking pens: `ancestorChain`, `penAtDepth`, `displayDepth` options, cycle checks. Screen Time depth labels are Category / App / Exact. Pure. | §12 |
| `tracking-summary.ts` | The one place time is totalled: occupancy, pen totals at a display depth, child drill, variant split/reach, tag totals, `withPrecision` (drop assumed blocks), `otherScopeOccupancy` (hours sitting in a different view). Grid, Activity Log, Day Log and Analytics all read it. | §12 |
| `entry-links.ts` | Joining scopes without coupling them. `companionsFor` (what every other scope says about one block's window), `attachCompanion` (paint the same window elsewhere), `applyPenLinks` (a pen's standing "Ian's House always means Social" rules), `suggestedCompanions` (pairings learned from the user's own history). Attachments are ordinary blocks, and only ever fill minutes the other scope left blank — a rule about one scope does not get to delete a statement about another. | §12 |
| `tracked-time.ts` | Tag rollups over the TimeGrid. `effectiveTagIds` is the rule everything else obeys: a block's tags are **every assigned pen's** standing tags **plus** any pinned to that block alone, so a youtube block that also counts as studying spanish feeds both pens' tags, and one zoo trip can count as exercise without the pen doing so forever. Minutes are unioned across scopes, so a minute tagged twice counts once. | §9, §12 |
| `pen-action-format.ts` | Default Done-today title templates on a pen. Picks the most specific template whose placeholders are filled; equal specificity keeps **list order** so rearranging formats is a real control. Pure. | §12 |
| `pen-action-sync.ts` | Bridge: painted blocks with `actionFormats` create/update/delete `loggedAction` Done rows (`pen-action-{id}`). User-edited titles are kept. Mount `usePenActionSync` on Tracking + To-Do. | §12 |
| `habit-tracking.ts` | Habit ⇄ tag link math: eligibility (`supportsTrackingLink` — Goal / Yes-No at daily, weekly, or monthly frequency), minute→hour conversion, and the idempotent `manualValue` / `trackedValue` split that keeps typed and tracked contributions apart. Pure, so `habits-store` can use it without a cycle. | §9, §12 |
| `habit-tracking-sync.ts` | The only module importing both stores: subscribes to the tracking store and pushes tagged minutes into linked habits (`syncTrackedHabits`, `syncTrackedHabitsForTask`, `useHabitTrackingSync`). Daily habits get that day's minutes; weekly/monthly habits get the sum across the week or calendar month. Starts only after both stores have hydrated so seed habits cannot be written over the vault. | §9, §12 |
| `sleep-log.ts` | The nightly sleep model, pure. Storage is still keyed by **the morning you wake into**, with signed minutes from that midnight (`-30` = 11:30 PM the evening before). Duration is then always `wokeMin - sleptMin`. Tracking types clocks **on the day on screen** (`placeAsleepOnDay` / `placeAwakeOnDay` / `sleepRowsForDay`): 1 AM Thursday is Thursday's night, 10 PM Thursday is Friday's. Morning Review still uses the noon-pivot `parseBedtime`. Also: `sleepIntervals`, `sleepStats` / `sleepTrend`, `typicalSleepWindow`. Contradictory ends return `null` rather than a laundered number. | §12 |
| `sleep-sun.ts` | Sleep onset vs **that evening's** sunset and wake vs **that morning's** sunrise, from persisted per-day sun — not today's clock on every night. Median offsets; copy room for later productivity/joy-around-sun. | §12 |
| `sun-times.ts` | Local NOAA/SunCalc sunrise and sunset for a `YYYY-MM-DD` + lat/lng. Pure. Two dates differ unless the geometry says they match. Persist is `sun-times-store.ts`. | §12 |
| `sleep-inference.ts` | Reading a night off the Tracking grid when the log is silent. Sleep is anything carrying the Sleep tag, in any scope, from a pen tag or a block tag. `resolveNights` reconciles stated and painted **per end** — a typed bedtime always wins, a missing one is borrowed and marked estimated — and reports the result's `source` so the UI can say where it came from. `nightFromGeneratedBlocks` reads a night back out of the blocks it produced, which is what makes a dragged block a correction. `straySleep` reports sleep no night accounts for — naps, second stretches — so it is neither averaged in nor lost. Blocks the log itself generated are ignored when inferring, so it cannot read its own output back as evidence. | §12 |
| `sleep-sync.ts` | The only module importing the sleep store *and* the tracking/task stores, and it runs **both ways**. Forward: a night becomes painted Sleep blocks (stamped `generatedBy.kind === "sleep"`, so re-deriving never touches hand-painted time or Screen Time stamps), a deterministic Done row, and — through the ordinary tag path — habit minutes. Back: `reconcileNightFromGrid` takes the grid's word when the user retimes, splits or deletes one of those blocks, so the tracker and Analytics cannot describe a night differently. A `deriving` latch keeps the two directions from chasing each other, and a night is only ever cleared from the log when there was somewhere to paint it in the first place. `useSleepSync` also re-derives every logged night once **both** vaults have hydrated, so a Tracking tab that mounts after persist cannot stay blank while Analytics → Sleep still shows the hours. `awakeWindowFor` hands a day's waking hours to `completion-window.ts`, degrading per end from stated → painted → the user's median night, and saying which. | §9, §12 |
| `screentime/` | ActivityWatch meaning layer (`README.md` in the folder). `app-categories.ts` maps apps → category roots; `map-events.ts` intersects window ∩ not-afk, drops short events, merges adjacent same-pen, optional web-watcher domains under Chrome/Safari; `sync.ts` idempotently replaces that day's `generatedBy.kind === "screentime"` blocks on the **Screen Time** scope only (never Activity, never iPhone views). Reachable AW + 0 blocks is success (`describeScreenTimeSync` says “no windows yet” vs “mapping dropped them”). `ensureAppPen` uses stable `st-app-{slug}` ids and hashes `PEN_PALETTE`; existing parentId/color are not overwritten. Fetch: Electron `window.desktop.fetchScreenTime` or localhost `/api/screentime`. | §12 |
| `date-utils.ts` | App-wide date helpers: `formatDateKey`, `formatLocalDateKey`, week/month/year keys, `getWeekStartDate`, `getWeekString`/`parseWeekString`, `getPrecedingWeekStarts` / `getPrecedingMonthStarts` / `getMonthDates` (habit period grids), range formatting, `isToday`, `startOfLocalToday`, `isPastLocalCalendarDay` (`day < startOfLocalToday`; independent of a selected/viewed date), safe date guards. | §7, §9 |
| `item-utils.ts` | Item-record helpers, and **the one answer to "what is this called"**: `itemTitle(record)` prefers the canonical `Item.title` and falls back to the legacy `Task.description` mirror, `itemTitleOrUntitled(record, fallback?)` labels the nameless. Every display read goes through these rather than reaching for either field, so the two spellings cannot disagree ([`docs/CANONICAL_FIELDS.md`](../docs/CANONICAL_FIELDS.md)). Its write-side twin `syncTitleFromDescription(next, previous?)` — applied by `task-store`'s `addTask`/`updateTask` — fills in a missing `title` and follows a rename made through `description` alone, but only when the two were already identical, so a parked note's body never becomes its name. It never writes `description`. `item-title-reads.test.ts` scans `components/`, `lib/` and `app/` and fails on any new direct `description` display read, with a short allowlist for the name *editors* (they read and write the same field in lockstep) — that list should only shrink. Also `isTaskItem` (Next Actions **or** explicit `type === "task"`; missing type is not a task), `createListItem` (default type `"item"`), `createNextActionItem` (`type: "task"`), `countsInDone`, attribute seeding, `resolveCompletionPoints`. New signatures take `ItemRecord`. | §5, §6, §7 |
| `item-types.ts` | Item-type helpers: system `item` + `task`, `getItemType` (falls back to `item`), `resolveDetailView`, `mergeTypeRegistry`, schema composition, rule evaluation (`increased`/`decreased`/`changed`, `logAction`/`incrementHabit` effects). | §5 |
| `implied-actions.ts` | Execute type/list implied-action effects after an item update: log a completed Done-day action (`loggedAction`) and increment a habit. Title templates `{title}` `{delta}` `{attrId}`. | §5, §9 |
| `item-type-recipes.ts` | Optional starter schemas + implied-action hint copy for the type editor (Book, Furniture, Resource, Shopping, Progress → Done). | §5 |
| `catalog-types.ts` | Catalog seeds: Furniture, Resource, Shopping item. | §5 |
| `book-types.ts` | Catalog **Book** (cover, pages read, implied-action rules for Done + the default pages/day habit). | §5 |
| `flight-types.ts` | Catalog **Flight** (airline, airports, times, layovers, booked flag). | §5 |
| `migrations.ts` | Versioned data migrations for the unified Item model — v7 backfills `title`/`tags`/`links` and does not invent `type`; v11 fills in a `title` for records that still have only a `description`; **v12** fills a missing `type` (Next Actions or inbox → `"task"`, else `"item"`). Never overwrites an explicit type or a name a person typed. | §5 |
| `completion-status.ts` | Keeps the legacy boolean `completed` and the richer `CompletionStatus` (active/partial/deferred/cancelled/missed/done) in sync via the single invariant `done ⇔ completed`. `"missed"` is too-late: `isClearedFromWork` (done or missed) drops the row from To Do / Next Actions. Pure `withStatus`/`withCompleted` + status labels; callers persist through `updateTask`. | §6 |
| `available-tasks.ts` | Unmet-dependency predicate (`hasUnmetDependencies` / `isAvailableNow`). Shared by Scheduler `getAvailableTasks` and To Do Available now. Ghost ids do not block. Done **or** missed-opportunity deps do not block. | §7, §8.4 |
| `completion-events.ts` | Tiny pub/sub completion event bus (`onTaskCompleted`/`emitTaskCompleted`/`requestTaskCompletion`). `task-store.updateTask` emits on every false→true completion so the global popup appears. Checklist ticks call `requestTaskCompletion` first (`pending: true`) so Undo / dismiss leave the box unchecked. | §6 |
| `list-tree.ts` | Pure nested-list (sublist) forest helpers over `List.parentListId`: ancestors/descendants, render-tree builder, and cycle/dangling-parent-safe `moveList`. | §6.2 |
| `list-links.ts` | Directed **connected-list** membership (`List.linkedTargetListIds`, not nesting): expand `Task.lists`, honor `listMembershipExclusions`, symmetric settings view, unlink without mass-delete. [`LIST_LINKS.md`](../components/Lists/LIST_LINKS.md). | §6 |
| `molecular.ts` | Pure, immutable `Subtask[]` tree helpers for "molecular" task decomposition — recursively split a task into atomic (`isMolecular`) steps, each with a self-contained `context`. Backs the "Just Start" focus flow. | §1/§59/§128 |
| `priority.ts` | Transparent, entropy-aware priority formula: weighted blend of urgency/importance/cognitive-load/entropy with `priorityBreakdown` so the UI can explain *why* a task ranks where it does. `DEFAULT_PRIORITY_WEIGHTS`. | §6 |
| `scheduling.ts` | Canonical schedule-field helpers (`scheduleFieldsForPeriod`, `clearedScheduleFields`) shared by the Scheduler UI and the scheduling service. | §7 |
| `smart-parse.ts` | Pure, LLM-free smart-capture parser: leading colon paths (`list: item`, `folder: list: item`), `parsePathHeader` for Bulk Add, plus dates/times/priority/duration/`cat:` hints and `-mb` / `-monkey` (Monkey brain) → `SmartSuggestion` + highlights. `now` injectable. | §4, §10 |
| `inbox-batch.ts` | Inbox list order (`sortInboxNewestFirst`), walk queue (`walkQueueIds` = current selection only, `nextWalkId`, `rotateInboxQueue`, `renameInboxIdea`) + multi-select targets + batch list/deadline/delete/clarify/partition patches (`deleteInboxItems`, `clarifyInboxItems`, `setInboxMonkeyBrain`, `inInboxPartition`, `inboxAllSelected`). Monkey brain is `Task.monkeyBrain` on `stage: "inbox"`. Merge stays in `item-merge.ts`. Keyboard chords for `components/inbox.tsx` (`a` all, `u` none, `y` mark clarified, `b`/`i` move piles, `e` bulk edit, `#` delete). | §4.4, §4.5 |
| `inbox-credit.ts` | +1 point for clarifying or discarding an Inbox idea; +50 each time the **revisit** Inbox reaches 0 open ideas (Monkey brain does not count). Writes `points-store`. | §4.4, §14 |
| `inbox-recent-lists.ts` | Persist last-used list ids (`inbox-recent-list-ids`) + suggest them (then newest item lists) for the Walk picker. | §4.4 |
| `capture-target.ts` | Resolve/create folders+lists from a capture path and build the task (Inbox vs skip-Inbox / All Items). `-mb` / `-monkey` forces Inbox and sets `monkeyBrain`. Used by Quick Add and Bulk Add. | §4 |
| `apple-notes.ts` | Apple Notes ingest: period ranges, HTML strip, preview/snippet/body fetch via Electron `window.desktop.fetchAppleNotes` or localhost `/api/notes` (same `electron/apple-notes.js` reader), bulk-add draft/parse (`parsePathHeader` headers — `Folder: List:` creates a new folder by name), park-to-**notes to ingest** (`ensureIphoneNotesIngestDestination`), Telegram Shortcut park-to-**iPhone Notes Store** / **Parked** (`ensureIphoneNotesStoreDestination`, `iphone:` ids), skip already-ingested ids. UI: `notes-ingest.tsx` + `iphone-notes-store.tsx`. | — |
| `ingest/` | Phone-message ingest (Telegram first): dedupe, parser, text triggers, shortcuts, capture/habit/tracking/grocery/`needed:`/`get:`/notes writes, discrete events + activity spans (`generatedBy.kind === "text"`), **iPhone Screen Time / Calls / Texts** (`apply-phone-screen.ts`, `apply-phone-life.ts`), list/folder/inbox **read-back**, pairing, log store, always-on hub, receipt OCR + journal/PDF Docs scans. Grocery verb **`groc`** (bare `g` retired). See [`ingest/README.md`](ingest/README.md) and [`docs/MESSAGE_INGEST.md`](../docs/MESSAGE_INGEST.md). | §4 |
| `app-navigation.ts` | App-wide navigation persistence: localStorage keys + `readStoredTab`/`writeStoredTab`, optional ids, JSON records, calendar dates, and `ui-scroll` offsets so a refresh returns the user to their last tab/folder/period/view **and** scroll (Docs `docs:<id>`, folder homepage, sidebar, Analytics canvas, Operations `ops:<id>:<panel>`). `writeStoredTab` also POSTs the pin to `/api/persist` on localhost so the shared file does not keep an older Home tab (it was stuck on Tracking while this profile had already switched to Habits). | — |
| `use-persisted-tab.ts` | Restore a Radix tab after mount so the stored value wins over the SSR fallback. | — |
| `use-persisted-scroll.ts` | Restore a scroller's `scrollTop` after remount / refresh (`ui-scroll` slots). Skips the `scrollTop: 0` collapse when a Radix tab hides. | — |
| `use-current-date.ts` | Shared Home calendar cursor. Persists the chosen day; midnight advances only while that cursor is still today. | — |
| `search.ts` | Pure, framework-free ranked global search: `searchItems(query, items, opts?)` → `SearchResult[]` (`{ item, score, matchedOn }`), case-insensitive multi-term AND over title/tag/attribute tiers, deterministic ordering, plus `displayTitle` and the `SearchResult`/`SearchField` types. Backs the Cmd/Ctrl-K palette. Unit-tested. | §6a |
| `needs-attention.ts` | Pure selector `getNeedsAttention(tasks, opts?)` + grouping, reason labels, and `clarifyNeedsAttentionItem` / `splitNeedsAttentionItem`. Flags overdue / unclarified / blocked / stale plus `neglected` (goals / operations / list items, reusing `goalsNeedingAttention` + operation logs) and `zombie` (`daysPushed` / `weeksPushed` / high entropy). Unit-tested. | §6b |
| `pending-reviews.ts` | `getPendingReviews`/`countPendingReviews` — which end-of-period reviews (day/week/month/quarter/year) are still due for the previous period. | §13 |
| `affirmations.ts` | Helpers for the Morning **spoken affirmations** ritual: locate/seed the Lists "affirmations" list (`findAffirmationsCategory`, `DEFAULT_AFFIRMATIONS`), read its lines (`getAffirmationItems`/`affirmationText`, resilient to the category→list rename), and Fisher–Yates `pickRandom` a session subset. Unit-tested. | §13 |
| `morning-todo-walk.ts` | Six-slot morning to-do walkthrough grammar (`tier duration points importance resistance excitement`); dash = leave unchanged; applies tier/duration/points + `dayRatings` + append-only `resistanceReadings`. Shared by BIM `gm` and desktop Morning Review. | §13 |
| `vocal-confidence.ts` | Pure, dependency-free **vocal-confidence** DSP + scoring for the affirmations ritual. McLeod-Pitch-Method `detectPitch` (NSDF + parabolic interpolation; tracks female & male f0), `computeFrameMetrics`, jitter/shimmer (`relativePerturbation`), HNR (`clarityToHnr`), terminal contour/loudness (uptalk + trailing-off), and `scoreConfidence`/`ConfidenceTracker` → a live `ConfidenceScore` (volume · steadiness · conviction · sustain) with hard gates. Swappable scoring boundary; unit-tested. See `components/Reviews/README.md`. | §13 |

### Second-brain helpers

Pure, serializable building blocks for typed links (sources → beliefs, notes,
relations). No store/React imports.

| File | Purpose | Spec |
|------|---------|------|
| `links.ts` | Typed-relation catalog + tag helpers: `RELATIONS`/`inverseRelation`, evidence relations (`supports`/`refutes`…) with per-link `stance` (`getLinkStance`/`linkStanceWeight`), `addLink`/`removeLink`/`hasLink`/`recordLinks` (self/dup-guarded), `normalizeTag`/`addTag`/`removeTag`. Graph = `Item.links` on each `ItemRecord`. Distinct from `Task.dependencies` / `parentTaskId`. Backs the store's tag/link selectors. Unit-tested. | §5 |
| `graph-layout.ts` | Pure, deterministic graph/timeline geometry: layered (Sugiyama-lite) `topologicalRanks` for Gantt/dependency columns. SSR-safe. | §14/§15/§19 |
| `second-brain-types.ts` | Declares the **Source** and **Belief** `ItemTypeDefinition`s (attributes, defaults, detail panels) + a `withSecondBrainTypes` seed helper. Exposes `SOURCE_ATTR`/`BELIEF_ATTR`/`DEFAULT_SOURCE_TRUST`. | §10/§55/§66 |
| `note-types.ts` | Declares the `note` (rich-text document) `ItemTypeDefinition` + `withNoteType` seed helper. Notes aren't actionable but participate in tags/links/lists; Docs tab uses HTML `Item.body` plus `docsFolder` / `docsFontFamily` / status attributes; ItemDetail `"body"` panel still supports markdown notes. | §5 |
| `belief-strength.ts` | Pure derived **belief strength** (0–1, 0.5 = balanced) combining each evidence link's `stance` with its source's `trust`, weighted by total evidence mass. Backs the belief calibration view. Unit-tested. | §10 |
| `event-links.ts` | Pure event-prerequisite checklist helpers: link a `CalendarEvent` to its `checklist-of` tasks and derive each task's `schedulingConstraints.mustBeDoneBefore` from the event's start. Unit-tested. | §7.5 |

### Operations helpers

An **Operation** is a `Task` with `type: "operation"` — a little graphic tool for
any kind of project (its work, ideas, data, and progress). Categories and panels
decide how it is filed and which workspace it shows. Completed and inactive
operations (`done`, `paused`, `abandoned`) stay off the home board until Show
archived. See [`components/Operations/README.md`](../components/Operations/README.md).

| File | Purpose | Spec |
|------|---------|------|
| `operation-types.ts` | The `operation` `ItemTypeDefinition` (mission, stage, **categories**, target date, home notes, **tracking tags**, part-formula JSON) plus the configuration model: the `OPERATION_PANELS` registry (home/to-do/phases/parts/timeline/locations/plan/resources/log + the queue rail; the To do tab keeps the `tasks` id), `OPERATION_PRESETS` (Standard/Blank/Trip/Project/Paid job), panel normalizers with the `activities`→`locations` / `itinerary`→`timeline` renames, category normalizers, and `getOperationTrackingTagIds` (the join to Tracking/Habits). `withOperationType()` seeds the registry. Unit-tested (`operation-model.test.ts`). | §5 |
| `operations.ts` | Pure operation helpers: hours rollup over `timeLogs`, relation resolution in both link directions (`has-phase`/`has-part`/`has-resource`), phase completion + overall progress, work/neglect heatmap, `selectToDoNext` ranking, `isArchivedOperation` (paused / done / abandoned / completed), and the home board's `selectOperations`/`collectOperationCategories`/`groupOperationsByCategory`/`filterOperationsByCategory`/`sortOperations`. Unit-tested. | §5 |
| `operation-parts.ts` | Part kinds (formulas), instances, ideas, glance meters, and `collectOperationTodoTasks` (list items ∪ phase steps ∪ part stage/finish tasks). Ideas are not tasks. Unit-tested. | §5 |
| `operation-lists.ts` | Operations ↔ Lists bridge: one real `List` per operation (`op-list-{id}`) filed in an **Operations** folder, backing the workspace's **To do** panel so operation work is ordinary items (item types, attributes, search, All Items). Lazily created, renamed with the operation. Phase steps and part tasks are filed onto it. | §5, §6 |
| `operation-itinerary.ts` | Backing workspace module for the optional **Timeline** / **Locations** / **Plan** panels (day grid, places map, plan doc). Created only the first time one of those panels is opened. Unit-tested. | §8 |
| `operation-work-session.ts` | Live **"working on this now"** clock: splits a session onto local days, paints Tracking minutes with a shared `spanId` so a session that crosses midnight stays one continuous block, writes a `loggedAction` Done row (`worked on {name}`) plus `timeLogs`, and (via the operation's Tracking tags) feeds linked daily habits. `tickWorkSession` grows the grid block while the session is open and is silenced from undo; start/stop are one Cmd/Ctrl-Z step each. Unit-tested. | §5, §9, §12 |
| `focus-timer-log.ts` | Module focus timer complete → `timeLogs` on Working Now's item (or a picked item). Reuses day-slice math from `operation-work-session`; writes via `taskRepository`. Does not paint Tracking, stop Working Now, or pick 52:17 from load. Unit-tested. | §5, §8 |
| `work-session-store.ts` | Persisted pointer to the open Operations session so a refresh does not forget you started. | §5, §12 |
| `pen-color-session.ts` | Live **"working on right now"** for a Tracking pen: the clock starts at the current second, the block is that pen's color in that pen's view (minute slices, shared `spanId` across midnight). Does not write Done or `timeLogs`, and does not stop an Operations session. Ticks are silenced from undo; start/stop are one Cmd/Ctrl-Z step each. Unit-tested. | §12 |
| `pen-color-session-store.ts` | Persisted pointer to the open pen-color session. | §12 |

### Planning, objectives & analytics helpers (Brain2)

Pure, unit-tested computation behind the Analytics, Scheduler, and Objectives
views. All take plain data in / return plain data out (no store, no LLM).

| File | Purpose | Spec |
|------|---------|------|
| `objectives.ts` | Objectives & Goals helpers: period keys (`periodKeyFor`), objective prioritization (`isObjectivePrioritized`/`prioritizedObjectives`, `MAX_PRIORITIES_PER_PERIOD`), goal progress (`goalProgressFraction`/`goalProgressPercent`), and "direction in life" coverage (neglected goals, days serving no goal) derived on read from each task's contribution fields/typed links. | §10 |
| `plan-vs-reality.ts` | Compares a period's plan (plan-text + scheduled tasks + points) against what happened across tasks/time/points dimensions, rolled into an "intention → outcome" variance score (0–100). | §33 |
| `calibration.ts` | Estimate-vs-actual calibration over completed tasks: per-task points (ratio, signed error %), aggregate bias/under-over rates with a plain-language insight, ratio-bucket histogram, and per-period median-ratio trend. | §26/§29 |
| `critical-path.ts` | Dependency-free CPM + PERT solver over a task network: forward/backward pass, slack, and the zero-slack critical path (`isCriticalEdge`). Durations from `pertEstimate` `(o+4l+p)/6` else `estimatedDuration`. `wouldCreateCycle` / `findCyclePath` check one proposed dependency without changing solver results. | §14/§15/§19 |
| `decision-matrix.ts` | Weighted multi-criteria decision analysis (MCDA): min–max normalizes each criterion across options (benefit vs cost), renormalizes weights, ranks options highest-first with tie handling. Backs the Modules "decision-matrix" view. | §5 |
| `metrics.ts` | Classical analytics over `{date,value}` series: least-squares trend + rolling slope, Pearson correlation (date-aligned), windowed change-point detection, context-switch counts, Shannon entropy, Gini, Herfindahl–Hirschman, lag-k autocorrelation, naive DFT periodogram, coefficient of variation, empirical survival of a current stock. Dates parsed as local days. | §15 |
| `overcommitment.ts` | Daily `daysPushed` reconstruction + logged-minute series over a date window; `trend` / `rollingSlope` / `detectChangePoints` → one sentence + n. Analytics → Overcommit. Does not reschedule. | §15 |
| `streaks.ts` | Store-agnostic streak computation (`day`/`week`/`month`) over date-keyed completions → current/longest run, last date, active-period count. Powers habit/review/focus streaks. | §8b/§9.5 |
| `spreadsheet-utils.ts` | Pure grid/rollup math: numeric/currency attribute detection, `aggregateColumn`/`sumBy`, currency-aware `formatNumber`, and `rollup` (group-by + sum) for the spreadsheet display and Module summary views. Unit-tested. | §5 |
| `spreadsheet-contract.ts` | The framework-free read/write/sort/filter contract every SheetGrid consumer shares: `SheetColumn` (including built-in fields), the serializable `SheetViewConfig` (sort/filter/freeze/widths/row-heights/`columnIds`), `sortRows` (blanks always last), `persistSheetViewConfig` / `columnWidthsByList` (`List.sheetConfig.columnWidths`), and the `canWriteCell` formula-column write guard. Unit-tested. | §5 |
| `spreadsheet-catalog.ts` | Attribute → spreadsheet column catalog: on-this-list first, vault attrs, built-in Task fields, per-list `columnIds`, assign-to-list. Shared with Details. Unit-tested. | §5 |
| `spreadsheet-keys.ts` | Pure grid interaction model behind the Google-Sheets ergonomics: keyboard navigation (`moveActive`/`tabTarget`/`enterTarget`), rectangular range math (`normalizeRange`/`isWithinRange`/`rangeArea`), clipboard TSV (`rangeToTSV`/`parseClipboardGrid`), and `selectionStats` (Sum/Avg/Min/Max/Count). Unit-tested. | §5 |
| `spreadsheet-paste.ts` | Expand a pasted TSV grid into cell writes (block paste vs single-value tiling across a selection). | §5 |
| `spreadsheet-file.ts` | Load an uploaded CSV/TSV/TXT or Excel (`.xlsx`/`.xls`) file into headers + rows for Lists import. | §6 |
| `sheet-a1.ts` | A1-notation cell-reference math: `columnToLetters`/`lettersToColumn`, `parseA1`/`formatA1`, `isCellFormula`, `extractA1Refs`, and `shiftFormula` — relative-reference rewriting for fill-drag that honors `$`-absolute markers and skips quoted strings. Unit-tested. | §5 |
| `sheet-eval.ts` | Evaluates per-cell `=A1` formulas (`=B2+C2`) against a `RawCellAccessor` over the grid: reuses the safe `lib/formula` engine (no `eval`), resolves A1 refs to other cells recursively with cycle detection, and formats the result. Unit-tested. | §5 |

## Data-access layer & domain services

A repository abstracts data access from storage so business logic and UI don't
reach into Zustand directly — the single seam for opportunistic **MongoDB Atlas**
sync via a future `RemoteDataSource`/`SyncingDataSource` (§3) and for sharing this
layer with a future mobile client. The local store stays the offline source of
truth. Domain services hold cross-entity workflows on top of the repository, and
Zod validates writes/imports at the boundary.

| File | Purpose | Spec |
|------|---------|------|
| `data/schemas.ts` | Zod schemas for `Task`/`TaskCategory`/`CategoryFolder`/`ItemLink`/`Subtask`/attribute values + the full store snapshot; `parseOrThrow` and `ValidationError`. Permissive about optional/legacy fields — **except `itemLinkSchema`, which is strict**, so a field missing from it is dropped rather than passed through. `stance` + `weight` (belief graph) and `Subtask.isMolecular` + `context` (molecular steps) are listed for that reason. | §5 |
| `data/task-repository.ts` | `taskRepository`: canonical CRUD + query API over the task store (item records); validates writes via Zod. Includes tag/link ops (`byTag`/`addLink`/`removeLink`). Implementation is still `task-store`. | §4–§7 |
| `services/completion-service.ts` | `completeTask`/`uncompleteTask`/`toggleCompletion`/`markMissedOpportunity`/`unmarkMissedOpportunity` — completion and too-late semantics incl. repeated-"count" tasks and actual-duration capture (points still awarded in the store on **done** only). Missed does not award points or fire the completion popup. Pushes onto `action-history.ts` so Cmd/Ctrl-Z reopens the item. The completion popup **Undo** calls `uncompleteTask` and then `removePointsForTask`. | §6 |
| `services/scheduling-service.ts` | `scheduleTask`/`scheduleTaskToTime`/`clearScheduledTime`/`unscheduleTask`/`pushTask` — scheduling workflows delegating to `scheduling.ts` + `item-utils.ts`. | §7 |
| `services/review-service.ts` | `savePeriodReview`/`getPeriodReview`/`upsertPeriodReview` — period-review persistence over `useReviewsStore` (stable id `${period}:${key}`). | §6 |
| `workflow-hooks.ts` | Dependency-free mutation seam: `dispatchItemMutation`, `addItemMutationListener`. `task-store` fires create/update/complete. | §5 / §8 |
| `workflow-engine.ts` | Module workflow evaluation (`dispatchWorkflows`) with a re-entrancy cap. | §8 |
| `services/item-mutation-service.ts` | `initWorkflowEngine` on client mount: wires workflows **and** implied-action effects (`logAction` / `incrementHabit` via `implied-actions.ts`). Idempotent; skips `loggedAction` re-entry. | §5 / §8 |
| `data/backup.ts` | Full app backup/restore: snapshots every persisted store (including home layout, home weather, sun history, and the Names overlay), free-text plans, attachments, docs, and every other durable key for this profile (`extras`: item history, friend pins, module notes, navigation, ingested-note ids, …) into one JSON file (`createBackup`/`createFullBackup`/`downloadBackup`, filename `brain2-backup-*.json`, `app: "brain2"`; restore still accepts `app: "cogs"`). Split `brain2-*` / `cogs-*` copies of one vault are folded together, and a plan draft left only on a bare `monthPlan-*` / `dayPlan-*` / `weekPlan-*` key is written onto the `brain2-*` key restore reads. A hydrated store is snapshotted from the open window when the last disk write failed. Unprefixed relics (`friend-worn`, `task-storage`, …) ride in `extras`. Leftover `friend-pic:` local copies and gallery `data:image` URLs still on the open window are folded into `attachments` as `friend_<id>`. Doc export keeps the newer body when memory and IndexedDB disagree. `previewBackup` + `restoreBackup(backup, { storeKeys, mode, extras })` do per-store merge/replace. Replace rehydrates so the file's rows win over the open window. Merge brings an item back even when this window had deleted it (`removedTaskIds` does not tombstone a row the file still has). A full restore also replaces `extras`; an older file that lacks `extras` does not wipe them. Restoring the theme store rewrites **`brain2-pcb-mode`**. Restoring the habits store rewrites **`brain2-habit-led-tint`** and the grade-tube pins. `data/recovery-backups/` lists via GET `/api/recovery-backups` when that folder exists — the hub now drops `auto-*.json` snapshots there itself whenever a write shrinks or is refused (`scripts/persist-api.mjs`), so a vault that lost rows is one restore away. | §3.2 |
| `data/data-source.ts` | Phase 10/11 **groundwork** (not yet wired): the transport-agnostic, Promise-returning `DataSource` interface covering every persisted entity (tasks/categories/folders/reviews/points/plans) plus `transaction()` and `DataSourceError`. The seam for the future out-of-process backend. | §3 |
| `data/sources/local-data-source.ts` | **Groundwork.** `DataSource` impl backed by today's sync stores + `taskRepository` (each method wraps the sync result in a resolved Promise). Additive default/offline impl; `transaction()` runs inline (non-atomic). | §3 |
| `data/sources/ipc-data-source.ts` | **Groundwork.** Renderer-side `DataSource` skeleton that validates inputs (Zod) then forwards each op to the Electron main process over a typed `window.cogs` bridge (historical name; product is **BRAIN2**); inert until channels are wired (throws `"not wired"`). See `electron/ipc/README.md`. | §3 |
| `data/mongo/collections.ts` | **Groundwork (driver-agnostic).** Planned MongoDB collection names, document shapes (app `id` → `_id`), and index plan as plain TS types/constants — no `mongodb` import. See `data/mongo/README.md`. | §3 |
| `data/mongo/mongo-data-source.ts` | **Groundwork (driver-agnostic).** Main-process `DataSource` skeleton for MongoDB; methods throw `"not implemented"` with `TODO(phase-11)` notes. Driver injected later via constructor; shapes/indexes in `./collections.ts`. | §3 |
| `habit-utils.ts` | Habit type normalization (`GOAL`/`TIME`/`COUNT` aliases), `isHabitGoalMet` (optional date + `weeklyData` so climb targets resolve). Exemption is separate (`habit-exemption.ts`): waived is not met. | §9 |
| `attribute-utils.ts` | Legacy attribute type normalization and value coercion; run from `task-store` migrate on load. | §5 |
| `module-templates.ts` | Pre-built workspace **module templates** (Itinerary, House Cleaning/Tidy, Budget, Book Tasting, Film DNA Lab, GradSearch, Blank): `buildModuleTemplate` (pure — lists + attribute schemas + seed items + views) and `instantiateModuleTemplate` (commits to stores, then `syncModuleListContents` so Tidy/Trip shadow records become Module Lists items). Itinerary v2 seeds Plan (`doc`), Itinerary (`itinerary-doc`), Activities (`trip-map`), plus City Places; Film DNA Lab seeds the `film-dna` view + Films list; House Cleaning App seeds a self-contained `house-cleaning` (Tidy) view **and** projects it into Whole house / area lists; GradSearch seeds a single `grad-search` explorer over the bundled catalog (no lists). Unit-tested. | §8 |
| `module-lists.ts` | Auto-created **Module Lists** folder tree (`folder-module-lists` / `{Module} Lists`), hide-from-global-All, `createdByModuleId` filing. Item projection is `module-list-import.ts`. | §6, §8 |
| `module-list-import.ts` / `module-list-import-shared.ts` / `module-list-import-tidy.ts` / `module-list-import-trip.ts` | Project Tidy chores (areas as nested lists, priority/est/actual/done/subtasks) and Trip days/schedule into Module Lists items. Idempotent merge; Tidy remains write-source for mapped fields. Field map: [`components/Lists/MODULE_LISTS.md`](../components/Lists/MODULE_LISTS.md). | §6, §8 |
| `module-plan-sync.ts` | `syncModuleToPlan` — appends a workspace's finalized, dated items as a new day Plan entry (`plan-text.ts`). | §7, §8 |
| `itinerary-assemble.ts` | Pure day-block assembly for printable itineraries (Trip Plan + Flights + Entries → left meta / right schedule / sleep row). Unit-tested. | §8 |
| `itinerary-migrate.ts` | Best-effort upgrade of older Itinerary workspaces to the v2 view set (doc / itinerary-doc / trip-map). Unit-tested. | §8 |
| `trip-itinerary.ts` | Self-contained trip days on `module.config.tripItinerary`: start/end auto-days, city/travel modes, timed plan/note/flight rows. Unit-tested. ⚠ **Shadow database — do not copy.** Days/flights are **projected** into Module Lists (`module-list-import-trip.ts`); two-way Item storage is still the target ([`docs/MODULE_PLATFORM.md`](../docs/MODULE_PLATFORM.md)). | §8 |
| `trip-activity-lists.ts` | Activities map buckets + city-chip dedupe (a place can sit on several filter lists). Unit-tested. | §8 |
| `trip-directions.ts` | Walking/driving/transit distance between map pins (OSRM + Google Maps URL; optional Directions API). Cached. Unit-tested. | §8 |
| `city-search.ts` | Open-Meteo city autocomplete → capitalized "City, Country" (or City, Region) labels. Cached via `api-cache.ts`. | §8 |
| `places-search.ts` | Place autocomplete + geocode (Photon by default; optional Google Places). Cached; Google details fetch in parallel. | §8 |
| `geocode.ts` | `parseCoord` + Open-Meteo URL helper. City/place geocoding is `city-search` / `places-search`. | §8 |
| `weather-client.ts` | Open-Meteo forecast + sunrise/sunset; geocodes via `searchCities`. Cached. Home overview uses `fetchHomeDayWeather` (current + hourly + 7-day daily) and `fetchHomeAirQuality` (US AQI). Tracking/Sleep sun clocks use `sun-times.ts` (persisted per day), not this 30‑min weather TTL. Unit-tested (`daylight.test.ts`, `weather-flight.test.ts`, `weather-home.test.ts`, `home-weather.test.ts`). | §8 |
| `tide-client.ts` | NOAA CO-OPS tides for the Home weather instrument. Station from widget beach pin or nearest catalog coast (San Diego / Ocean Beach → La Jolla Scripps `9410230`). Beach aliases + `coastPicksNear`. | §8 |
| `flight-lookup.ts` | Flight number → schedule estimate (optional Aviationstack; else editable estimate from day cities). | §8 |
| `parse-flight-text.ts` | Unstructured airline / boarding-pass paste → flight segments + layovers. Unit-tested. | §8 |
| `parse-event-text.ts` | Unstructured itinerary / tour-schedule parser → draft calendar events for Plan **Paste Events**. Unit-tested. | §7.5 |
| `filmrecs-types.ts` | Film DNA Lab catalog shapes (`FilmRecord`, shelves, attribute ids). | §8 |
| `filmrecs-catalog.ts` | Films-list tasks ↔ `FilmRecord` + Letterboxd merge helpers; seed from `filmrecs-seed.json`. | §8 |
| `filmrecs-score.ts` | Offline Film DNA scoring (decade vectors, Watch ranking, blend). Unit-tested. | §8 |
| `letterboxd-parse.ts` | Letterboxd CSV / export-folder parser (watchlist/watched/ratings/diary/`likes/films.csv`). | §8 |
| `house-cleaning.ts` | Tidy house-cleaning model (areas, stuck mode, plans) stored on `module.config.houseCleaning`. ⚠ **Shadow database — do not copy as a pattern.** Chores are **projected** into Module Lists (`module-list-import-tidy.ts`) so Lists/search can see them; Tidy is still the write source until two-way sync ([`docs/MODULE_PLATFORM.md`](../docs/MODULE_PLATFORM.md), [`MODULE_LISTS.md`](../components/Lists/MODULE_LISTS.md)). | §8 |
| `doc-html.ts` | Docs WYSIWYG HTML sanitize, selection styles, markdown→HTML migration, word count. Unit-tested. | §5 |
| `doc-links.ts` | URL detect / normalize / auto-linkify for Docs hyperlinks. Unit-tested. | §5 |
| `google-fonts.ts` | Allow-listed Google Fonts catalog + stylesheet loader for Docs. Unit-tested. | §5 |
| `image-resize.ts` | Client-side image downscale/compress for Docs uploads. Unit-tested. | §5 |
| `attachments.ts` | Blob store behind every attachment byte: IndexedDB `cogs-attachments` (`brain2-demo-attachments` on Demo), in-memory when IDB is missing (tests/SSR). A write stays in that memory map even if IndexedDB rejects it, so a full backup can still export the bytes. `FileValue.uri` and `image` / `multiimage` attribute values hold **`idb:<id>`**, never the bytes. `migrateTaskFileValues` + `migrateTaskImageAttributes` run on task-store rehydrate and lift legacy `data:` URLs out of the Lists blob — a single 592KB PNG pasted into a custom image attribute was 1.2MB of a 5MB origin, and *every* vault's writes failed for want of that room, Tracking included. `putImageDataUrl` is the write path; `hooks/use-attachment-src.ts` resolves a ref for `<img>`. Unit-tested. | §5 |
| `pdf-to-html.ts` | pdfjs PDF → editable Docs HTML (best-effort for text PDFs). | §5 |
| `folder-tree.ts` | Nested folder sidebar helpers: tree build/flatten, editable-folder guards, scheduled-folder sort. Unit-tested. | §6 |
| `folder-membership.ts` | List↔folder filing: direct `Folder.listIds` (many-to-many), inherited ancestor folders for **Show nested**, cycle/scheduled guards. Not `parentListId`. See [`LIST_FOLDERS.md`](../components/Lists/LIST_FOLDERS.md). | §6 |
| `item-selection.ts` | Add/move/remove items across lists. `clearItemsFromList` strips one list id from every item (membership only; does not delete). List Settings **Clear list** then `updateTask`s so connected-list exclusions stick. | §6 |
| `append-log.ts` | Shared **append log**: immutable `{id, createdAt, text}` entries in a `v: 1` JSON envelope (optional unsubmitted `draft` for Plan). Stamp `9/20 9pm`; List / Bulk / Latest (`appendLogView`, fallback `planTextView`); leftover plaintext becomes one "earlier" entry. Used by Plan and Tracking day notes. | §7, §12 |
| `plan-text.ts` | Plan period keys on the shared append log (`dayPlan-*` / `weekPlan-*` / `monthPlan-*`). Writes through `cogsStateStorage` (Chrome persist hub; Electron may POST these unprefixed keys so an empty hub tombstone cannot re-seed). Unsubmitted composer text is `draft` on the same key. Empty string values are skipped. Month keys canonicalize to local `YYYY-MM` (unpadded `YYYY-M` migrates). Week keys accept both Monday–Sunday ranges and ISO `YYYY-Www`. Target: MongoDB `plans`. | §7 |
| `folder-all-items.ts` | Per-folder **All Items** (`__all-items__{folderId}`) plus Home All prefs (`__all-items__root`): ensure records exist, assign uncategorized folder items, folder drop helpers. View prefs persist on those backing lists. See [`FOLDER_ALL_ITEMS.md`](../components/Lists/FOLDER_ALL_ITEMS.md). | §6 |
| `scheduled-lists-sync.ts` | Keeps Next Actions period To Do smart lists and scheduled folder lists in sync (`na-smart-daily` / weekly / monthly). Archive lists **Completed** / **Missed Opportunities** are created here; membership is `lib/archive-lists.ts`. | §6, §7 |
| `archive-lists.ts` | Next Actions Completed / Missed Opportunities as real lists: resolve/reuse by name, join/leave `Task.lists`, honor exclusions. | §6 |
| `checklist-checkbox-vars.ts` | Checklist columns: default Completed only; extra ticks (Missed opportunity) opt-in per list. | §6 |
| `details-columns.ts` | Details table extras: `resolveDetailsColumnIds` on `List.detailsColumns`. Reuses `spreadsheet-catalog` (one attribute map); never reads `sheetConfig.columnIds`. Undefined = current Details columns; `[]` = Name only. | §6 |
| `default-view-prefs.ts` | Default reading-row chrome: unset = built-in pip/orb/type/date/chips; `List.defaultView` custom show/hide, extra compact meta, comfortable/compact. Independent of Details and Spreadsheet columns. | §6 |
| `csv.ts` | Dependency-free CSV/TSV parser for Lists spreadsheet import (quoted fields, delimiter detection). | §6 |
| `lists-grid-entries.ts` | `buildGridEntries()` — builds folder/list grid entries for Lists navigation (Map-keyed, no duplicate kind-id entries). Used by `enhanced-list-view.tsx`. | §6 |
| `lists-icon-grid.ts` | `computeIconGridPositions()` — deterministic x/y grid layout for the freeform Lists icon view, shared by the store and the auto-organize animation. | §6 |
| `string-utils.ts` | `hashString`, `hashIconSlot` — deterministic hashing for orb selection, icon slots, and connector mock seeds. | — |
| `api-cache.ts` | In-memory TTL cache + in-flight coalescing + `mapPool` for external API reads (city/place/weather/tide/route). Empty/null misses expire in 30s so a network blip does not stick. | — |
| `mobile-sync.ts` | HTTP client for the optional mobile hub (`/api/sync`). Manual push/pull from Settings and `/mobile`. | — |
| `remove-background.ts` | Orb/gem studio knockout, plus **photo** subject mode for today's friend (border-colour flood, edge stop, keep the animal). | — |
| `orbs-manifest.ts` | Auto-generated manifest of PNG orb filenames under `public/orbs-removebackground/`. Do not edit by hand. | — |
| `gems-manifest.ts` | Every PNG in `public/gems-removebackground/` (114 files; tight-cropped via `scripts/crop-gems.py`; `scripts/process-gems.py` rescans the folder). | — |
| `habit-gems.ts` | Furniture gem slots + defaults + persist sanitize; per-habit jewels via `pickRandomCatalogGem` / `ensureTaskGem` / `stampMissingTaskGems` / `resolveTaskGem` (stored gem, never type/category). `seedRng` stamps default habits with SSR-stable jewels. | — |
| `habits-control-panel.ts` | Compact width for the Home Dashboard Habits Tab Control Panel. Seam resize retired; leftover persist widths pin to 196px. Physics enlarges Willpower gems. | — |
| `willpower-stones.ts` | One Willpower gem satellite **per daily completion** on the visible week (same gem, unique `habitId:YYYY-MM-DD`). Uncomplete a day and that copy leaves. Row invert while any hit this week remains. Derived from completions (no extra persist). | — |
| `willpower-physics.ts` | Bouncing-ball lab for Willpower gems (no matter.js). Vertical g, e = −v⁺/v⁻, grab/lift/throw (kinematic `held`; z follows a lift off the plate; airborne gems skip the oval fence), airborne pass-over between gems, shallow-dish κ, glassy bounce, per-stir seed, calmer default whirl (`stirSpeed` 220 / `stirHop` 160 / `chaos` 0.22), extra instruments (ΣE, |p|, spin K, T_κ, bounces), editable knobs in a Physics popup with a live twin plate. Crystal is a **solid** in XY (gems bounce off; they do not tunnel) and a PNG occluder (Y-sort). Gems keep their photographed shape and may paint past the rim. Oval fence ~1.32×. | — |
| `vault-guard.js` | Record counts + shrink/seed guard shared by the persist hub, Zustand adapter, and Electron preload. Local wins unless it would wipe more than half the hub. Habits `contentRev` refuses an older same-sized snapshot (a rename or a typed cell does not change the record count) and still lets a richer hub heal a seed wipe. **`VAULT_RECORD_FIELDS`** names the rows every vault holds and *is* the protected set — it replaced a hand-picked five, so the friend gallery, Goals, Reviews, Modules, Metrics, item types, Points, and Regret were open to any thin snapshot with no guard at all. Curated collections (Lists, Goals, the gallery…) need `MIN_GUARDED_RECORDS` (8) rows before a halving counts as a wipe, so deleting 1 of 2 cards is not refused and then handed back on the next read; append-only logs (`log: true` — Tracking, Sleep, Reviews, Metrics, Points, Regret) are guarded at any size. A drop to *zero* is refused from two rows up. **`shouldRejectHubLogShrink`** additionally keeps the shared file *growing* for logs — a launch POST of 173 tracking rows over 216 is 80%, nowhere near the shrink guard, and the next profile to seed from the file inherits the loss. It is used only when writing the hub, never when reading: a profile that deleted a block is ahead, and handing it the fuller copy would put the block back every launch. Pref-only vaults are deliberately unguarded and listed in `vault-coverage.test.ts`. Empty strings are missing (plan-text tombstones). Dedicated Tracking day notes (`brain2-tracking-day-notes`) skip that key-count swap: once this profile has the map, it is the source of truth (a hub with more historical days must not replace today's jot). When a richer hub vault of *entries* wins, defined local `percentLedTint` / tube hues / `pcbMode` / `chromeFace` / theme `colors` overlay so an older hub blob cannot reset them. A higher local **`appearanceRev`** wins even when the pick looks like a seed default (ceramic / argon); the stamp is wall-clock (`appearance-rev.ts`) so it cannot regress across launches. **`shouldRejectAppearanceDowngrade`** blocks persist from writing ceramic / seed hues over a saved pick. **`stampAppearancePins`** copies a saved LED / tube / PCB pick onto tiny keys; **`applyAppearancePins`** paints those keys onto a *seed* hub read only — a blob with **`appearanceRev` > 0** stamps the pins from the blob instead. **`shouldSkipHubAppearanceCopy`** tells Electron preload not to replace a present theme blob or pin from the hub. Tracking overlay also copies `brain2-tracking-day-notes` onto a hub timegrid snapshot that dropped `dayNotes`. **`unionPersistSnapshots`** folds two Tracking or ingest logs so a Screen Time replace cannot drop a hand-painted block (generated rows refresh by batch; `removedEntryIds` is how a real delete stays deleted) and so a shorter Telegram event log cannot erase an older event. It also folds **Lists / Inbox** (`tasks` / `lists` / `folders` by id; clarifications and newer `createdAt` win; `removedTaskIds` tombstones a hard delete), **planned actions**, and **plan-text** append logs (`dayPlan-*` / `weekPlan-*` / `monthPlan-*`) so a desktop vault push that never saw a phone-hub Telegram write cannot drop those rows. **`overlayHabitCompletions`** keeps checklist cells the winning snapshot lacks, and a cell `updatedAt` beats a gem-stamp that only bumped `contentRev`. | — |
| `vault-coverage.test.ts` | Fails when a persisted store in `BACKUP_STORES` is neither record-guarded nor declared pref-only, and when a guarded vault's declared fields are missing from that store's live state (a mistyped field counts nothing, which looks exactly like having no guard). Every loss so far had this shape: a store shipped and nobody added it to the guard. | — |
| `persist-storage.ts` | Guarded Zustand persist adapter; Electron returns a rich local snapshot immediately and only waits on the hub for missing/seed-sized keys (~800ms cap) so Daily Habits is not blocked by a 2MB JSON parse. Identical payloads are skipped (no write, no hub POST). Empty strings are not vaults (`monthPlan-*` tombstones are skipped). Hub POST refuses Tracking/Sleep/Lists/Habits snapshots that shrink to less than half the stored records. Plan-text keys may POST from Electron so an empty hub month key cannot re-seed. `pickPersistItem` overlays local color / PCB prefs onto a richer hub vault. Late hub copies **re-read localStorage** (`choosePersistSnapshot`) so a plate or LED pick during the wait is not merged from the getItem-start snapshot. **`wroteWhileHubLoaded`** then abandons the follow entirely when the store saved during the round-trip: the old code decided from the getItem-start copy and painted it over the fresher one, then rehydrated, which is how tracking blocks typed during launch vanished from disk *and* screen. **`staleAgainstLocalVault`** re-reads at POST time so a second renderer (the module popout) cannot send the hub a vault this profile has already outgrown — the drop is far under the shrink guard, and the next launch reads it back. `setItem` refuses a theme/habits **appearance downgrade** (ceramic / seed hues over a saved pick) and **stamps** LED / tube / PCB pins from the new blob instead of re-applying a stale pin onto it. A late hub follow does not replace a present theme blob. A write failure is remembered **per vault** (`persistKeyFailed`): the next store to save something else no longer clears the banner for a key that is still unsaved, which is how a submitted day note looked filed and was not. QuotaExceeded also reclaims dead pre-prefix relics before retrying. Electron `userData` itself is pinned to Application Support/`cogs` (`electron/user-data-path.js`), not package.json `name` or the git folder name. Tracking bottom notes are not this blob — see `day-notes-persist.ts`. | — |
| `use-persist-hydrated.ts` | `usePersistHydrated` + `afterPersistHydrated` — wait for a Zustand persist snapshot (Habits sheet, tracking sync). | — |
| `utils.ts` | shadcn `cn()` helper (clsx + tailwind-merge). | — |

## Persistence outside stores

Some data still writes directly to `localStorage` from components or helpers rather
than through a Zustand store:

- **Tracking day notes** — `brain2-tracking-day-notes` (`day-notes-persist.ts`; `cogs-tracking-day-notes` alias). The bottom **append log** on Home Tracking. Written on submit; this profile's map wins over a larger historical hub copy. Each day's value is the shared append-log JSON envelope (`lib/append-log.ts`). Mirrored in memory on `brain2-timegrid-store.dayNotes` for older readers. The timegrid persist blob does not carry them on every submit. Reads **union both aliases** and seeds **union per day** (entries are immutable, so their id is identity); if that key's last write failed, the notes well says the jot is only in memory (`persistKeyFailed`) instead of showing a stamped entry that will not come back.
- **Screen Time prefs** — `brain2-screentime-prefs` (`lib/screentime/prefs.ts`; `cogs-screentime-prefs` alias). ActivityWatch URL, lookback, min duration, window-titles opt-in (off), last success, honest `lastSyncNote` (empty AW vs filtered vs painted). Not rewritten into the timegrid blob.
- **Plan text** — `plan-text.ts` keys (`dayPlan-*`, `weekPlan-*`, `monthPlan-*`): the same append log, keyed by period, hub-synced like other persist keys. Envelope may include unsubmitted `draft` so typing survives refresh before Submit. An empty string on those keys is a tombstone (the hub had `monthPlan-2026-09: ""`); hydrate skips it. Hub merges **union entries by id** so a Telegram `plan for rn` is not dropped by a thinner desktop Sync. Month identity is local `YYYY-MM` (`formatLocalMonthKey`); week identity is the Plan Monday–Sunday range (`getWeekString`) with ISO `YYYY-Www` copied on read. View mode `appendLogView` (`list` / `bulk` / `latest`; leftover `planTextView` still read).
- **Desktop companion** — `brain2-baby-animals-store` (gallery metadata + cute names + `friendHistory` + `personalities` + `friendMissions`, persist **v7** card-id identity), `brain2-friend-worn` (selected friend pin; unnamed pack cards still pin), `brain2-friend-dismissed` (union-only removed **card ids** / catalog species so a hub snapshot cannot resurrect them), and `brain2-friend-pic:<id>` (cutout bytes; `friend:<id>` on the card). `cogs-*` twins are copied, never deleted. Each gallery card is its `photo.id`; a name typed on one picture never moves to another. Hub/Electron snapshots merge **by id** and keep a local name when the hub copy is blank. **Preapproved pack:** `animalsrcs/` processed into `public/friend-pack/` (71 PNGs; 10 studio knockouts). **The pack and your own uploads are the only sources — no picture is ever fetched or invented from the web.** Cards ship unnamed so you name them in Gallery; shuffle wears another gallery card, and naming in the Gallery field names the next unnamed pack card. Pack is seeded once; deleted pack ids/URLs are not re-injected on Monday roll, refresh, or hub overlay. Naming a pack card “bunny” does not dismiss the catalog bunny. **Remove asks “Are you sure you want to delete …?”** **The worn friend persists across refresh** (pin + empty `weekKey` does not reshuffle; worn `displayName` is always the current card’s name) and only auto-changes on the first load of a new **Monday** week, or when the user shuffles, clicks a card, requests, or uploads. Returning friends may say **Hi again** / **hey there, remember me?** the first time they are assigned again — not on refresh. Header nest: **92px borderless** picture. Click it for a Stardew-style bubble (daily habit / today's To Do / Next Action / affection / whim; species quirks). **Click the bubble** for the mission sheet (**Open item** → item detail); **click the animal again** to decline and roll a new one. Finishing the asked work (or **I did it** on a whim) can grant friend points. **Escape**, the bubble **×**, or a click outside closes it without declining. **Gallery** (naming lives only there; **Details** = history + personality; equal cards; **navy** text-field focus; placeholder **Name this friend**). **Change picture** uploads your own photograph and vaults the cutout. Settings → **Baby animal friend**. Future clock / trinkets / listBias picker: [`docs/FRIEND_COMPANION.md`](../docs/FRIEND_COMPANION.md).
- **Legacy habits** — `habits-store` imports once from old `weekly-habits-*` keys.
- **Appearance pins** — `brain2-pcb-mode`, `brain2-habit-led-tint`, `brain2-habit-grade-tube`, `brain2-habit-output-tube`. Written when the matching store pick persists. A blob with `appearanceRev` > 0 stamps the pins from that blob; Electron preload does not copy a present theme blob or pin from the hub. A pick this page is also remembered on `brain2-pcb-pick` / `brain2-led-pick` (sessionStorage; cleared on a real reload by the boot script) so Fast Refresh or a late hub rehydrate cannot roll the plate or LED hue back. Hub POST merge keeps an incoming blob whose `appearanceRev` is higher instead of painting the previous hub plate onto it.

Brain2 is **offline-first**: `localStorage`, hydrated through the persist hub in
`persist-storage.ts`, is the source of truth, and the app is fully usable with no
network. MongoDB Atlas is a **future opportunistic sync target**, not a
replacement — the `DataSource` / repository seam is where it would attach, and
the shapes are sketched driver-agnostically in `data/mongo/collections.ts`. The
stray keys above are the ones that do not yet go through a store, which is the
thing worth fixing first; see `docs/SPEC_MAPPING.md` §3.

## Autogenerated values are always flagged

The app derives a lot of data the user never typed — how long a habit took, when
in the day it happened. None of it is presented as observed fact. Anything derived
carries a `FieldEstimate` on the record (`Task.estimates`, `lib/estimated-values.ts`)
naming the field, how it was produced, and the sentence explaining it. That buys
two things at once: records get filled in automatically, and the review can hand
the user a short list of "is this right?" instead of a wall of silent guesses.
A separate glance, `usualDurationMinutes`, medians observed sessions of the same
title or named type and paints **usually ~N** — it is never persisted as
`estimatedDuration` or a `FieldEstimate`.

Wave 13 (GS-1) grows `FieldEstimate` with an order — observed, recorded,
derived, inferred — and uses this same mark everywhere a number was computed
or guessed. Do not add a second provenance type.
[`docs/ScienceandSanityBrain2.md`](../docs/ScienceandSanityBrain2.md).

The rules that hold this together:

- **Observed beats assumed.** Painted Tracking minutes and a habit already measured
  in minutes/hours are recorded *unflagged*; a per-unit rate or a flat length is
  flagged. `precision: "definite"` on a habit's time estimate opts out.
- **Confirmation is sticky.** `canRegenerate` blocks any later sync from
  overwriting a value the user confirmed, which is what makes it safe to re-derive
  on every habit edit.
- **Never chase the clock.** A finish time is stamped once, when the goal is first
  met. Logging more later lifts the duration and slides the *start*, so the record
  does not rewrite itself while the user types.

## Notes

- Prefer `types.ts` + `calculations.ts` + `date-utils.ts` as canonical; older parallel files (`weekly-*`) have been removed.
- `task-store` runs attribute migration via `attribute-utils.ts` on load.
- Completion points for Lists/Next Actions items: `resolveCompletionPoints()` in `item-utils.ts` (default **1**, or numeric **Points** list attribute). On top of that, contributing to an Objective applies a **stacking multiplier** (1.5× default, or a prioritized objective's custom value) via the completion popup — see `goals-store.ts` (`taskObjectiveMultiplier`) and `components/Completion/`.
